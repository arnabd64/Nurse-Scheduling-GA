[project]
name = "workshift"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy<2.0",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "isort>=6.0.1",
    "ruff>=0.12.7",
]
